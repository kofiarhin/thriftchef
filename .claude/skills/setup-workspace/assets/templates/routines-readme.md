# Routines

Use this directory for proposed recurring operator work such as morning briefs, issue reviews, customer-note summaries, or review reminders.

For each routine define trigger, inputs, output, permissions, limits, and failure behaviour.

Start with read-only or documentation routines. Do not activate schedules or production-changing automation without explicit authorization.
